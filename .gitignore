# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual env
.venv/
venv/

# Runtime data (SQLite DB is created at runtime, never committed)
qguide_data.db
*.db
*.db-wal
*.db-shm

# Local server logs / tooling
.streamlit_run.log
.streamlit_err.log
.api_run.log
.api_err.log
.claude/

# Node / Next.js (web frontend has its own .gitignore too)
node_modules/
web/.next/

# Secrets (never commit)
.streamlit/secrets.toml
