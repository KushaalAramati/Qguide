# Runtime dependencies for the deployed Streamlit app (Streamlit Community Cloud
# installs from this file at the repo root). Pinned to the tested versions.
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
plotly==6.8.0
pydantic==2.13.4
dimod==0.12.22
dwave-samplers==1.8.0
